(function() {
    const styleId = 'gemini-ultra-design';
    if (document.getElementById(styleId)) document.getElementById(styleId).remove();

    const css = `
        /* 1. Fundo e Chat */
        .chat-history, .conversation-container, main {
            background-color: #f8f9fa !important; /* Um cinza bem clarinho elegante */
        }

        /* 2. Balão do Usuário (Você) */
        .user-query-container {
            display: flex !important;
            flex-direction: row-reverse !important; /* Foto na direita */
            align-items: center !important;
            gap: 15px !important;
            padding: 20px !important;
            margin: 15px 0 !important;
        }

        .query-content {
            background: #ffffff !important;
            border-radius: 20px 20px 0px 20px !important; /* Pontinha charmosa */
            box-shadow: 0 4px 15px rgba(0,0,0,0.05) !important;
            border: 1px solid #e0e0e0 !important;
            padding: 15px 20px !important;
            color: #333 !important;
        }

        /* 3. A Foto de Perfil (Clonando a do Google) */
        .user-query-container img, .user-avatar, img[src*="googleusercontent"] {
            width: 45px !important;
            height: 45px !important;
            border-radius: 50% !important;
            border: 2px solid #4285f4 !important;
            box-shadow: 0 2px 8px rgba(66, 133, 244, 0.3) !important;
        }

        /* 4. Balão do Gemini */
        .model-response-container {
            display: flex !important;
            flex-direction: row !important; /* Foto na esquerda */
            align-items: flex-start !important;
            gap: 15px !important;
            background: transparent !important;
            padding: 20px !important;
        }

        .model-response-text {
            background: #4285f4 !important; /* Azul Google */
            color: white !important;
            border-radius: 20px 20px 20px 0px !important;
            padding: 15px 20px !important;
            box-shadow: 0 4px 15px rgba(66, 133, 244, 0.2) !important;
        }

        /* Esconde elementos desnecessários para um visual limpo */
        .chat-input-container {
            border-radius: 30px !important;
            border: 2px solid #eee !important;
            margin-bottom: 20px !important;
        }
    `;

    const styleSheet = document.createElement("style");
    styleSheet.id = styleId;
    styleSheet.innerText = css;
    document.head.appendChild(styleSheet);

    console.log("Design Premium Ativado! ✨");
})();
